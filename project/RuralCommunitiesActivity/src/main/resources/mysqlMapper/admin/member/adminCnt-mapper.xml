<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javajo.sunshineRoad.mappers.admin.member.AdminCntMapper">

	<select id="getTotalCount" resultType="int">
		SELECT COUNT(*)
		FROM ADMIN
	</select>

	<select id="selectAdminDATECnt"  resultType="int">
		SELECT COUNT(*)
		FROM (SELECT *
			  FROM ADMIN
			  WHERE ADMIN_REG_DATE BETWEEN
						STR_TO_DATE(#{searchDTO.startDate}, '%Y-%m-%d %H:%i:%s.%f')
						AND STR_TO_DATE(#{searchDTO.endDate}, '%Y-%m-%d %H:%i:%s.%f')) result
	</select>

	<select id="appAdmin" resultType="int">
		SELECT COUNT(*)
		FROM (SELECT * FROM ADMIN WHERE ADMIN_AUTHORITY_ID IS NULL) result
	</select>

</mapper>