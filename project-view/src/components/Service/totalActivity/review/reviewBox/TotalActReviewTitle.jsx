import React from 'react';
import './TotalActReviewTitle.css';

export default function TotalActReviewTitle({ text }) {
  return (
    <div className="totalActReviewTitle">
      <p>{text}</p>
    </div>
  );
}
