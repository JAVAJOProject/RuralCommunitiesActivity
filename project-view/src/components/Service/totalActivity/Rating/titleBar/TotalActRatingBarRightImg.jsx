import React from "react";
import TotalActRatingBarRightImg from rl

export default function TotalActRatingBarRightImg({ imgSrc }) {
  return (
    <div className="totalActRatingBarRightImg">
      <img src={imgSrc} alt="RightImg" />
    </div>
  );
}
